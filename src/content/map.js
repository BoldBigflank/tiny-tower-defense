import { chest } from './models'

const MODEL_TYPES = {
    INTERSECT: 'intersect'
}

const mapObject = [
    {
        type: MODEL_TYPES.INTERSECT,
        pos: "0,1,0",
        drawings: chest
    }
]

export { mapObject }